CREATE TABLE USERS(
  USER_ID INT AUTO_INCREMENT PRIMARY KEY,
  FIRST_NAME NVARCHAR(16) NOT NULL,
  LAST_NAME NVARCHAR(16) NOT NULL,
  EMAIL_ADDRESS NVARCHAR(16) NOT NULL,
  PASSWORD NVARCHAR(16) NOT NULL,
  ROLE NVARCHAR(8) NOT NULL
);
